# Server Configuration
PORT=3001
NODE_ENV=development

# Security
# For Railway deployment: These secrets will be auto-generated using Railway's secret() function
# For local development: Generate using Node.js:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64').slice(0, 32))"
# 
# Railway syntax (replace these when deploying to Railway):
ENCRYPTION_KEY=${{secret(32, "chars")}}
API_KEY=${{secret(48, "chars")}}
ADMIN_PASSWORD=${{secret(32, "chars")}}

# Database Configuration
# For local development with SQLite:
DATABASE_URL="file:./data/database.db"

# For Railway production with PostgreSQL:
# Service name: Postgres-Research-Space
# Private URL (recommended - faster, no egress fees, more secure):
# DATABASE_URL="${{Postgres-Research-Space.DATABASE_URL}}"

# Alternative - Public URL (for initial testing or external connections):
# DATABASE_URL="${{Postgres-Research-Space.DATABASE_PUBLIC_URL}}"

# Additional PostgreSQL variables (auto-populated by Railway):
# PGUSER="${{Postgres-Research-Space.PGUSER}}"
# PGPASSWORD="${{Postgres-Research-Space.POSTGRES_PASSWORD}}"
# PGDATABASE="${{Postgres-Research-Space.PGDATABASE}}"
# PGHOST="${{Postgres-Research-Space.RAILWAY_PRIVATE_DOMAIN}}"
# PGPORT="${{Postgres-Research-Space.PGPORT}}"

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Logging
LOG_LEVEL=info
